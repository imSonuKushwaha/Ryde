import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const SignUp = () => {
  return (
    <SafeAreaView>
      <Text className="self-center">SignUp</Text>
    </SafeAreaView>
  );
};

export default SignUp;
