import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Rides = () => {
  return (
    <SafeAreaView>
      <Text className="self-center">Rides</Text>
    </SafeAreaView>
  );
};

export default Rides;
