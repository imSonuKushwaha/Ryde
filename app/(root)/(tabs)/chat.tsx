import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Chat = () => {
  return (
    <SafeAreaView>
      <Text className="self-center">Chat</Text>
    </SafeAreaView>
  );
};

export default Chat;
